# import math so we can import the pi funct
import math
from math import pi
# use input function to take response from user 
radius= float(input("Enter the radius of the circle:"))
# under area variable we enter the forumla which is shown below
area= pi*radius**2
print("The area of the circle is:",area)
